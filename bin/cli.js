#!/usr/bin/env node
'use strict';

/**
 * CLI entry point for subprocess mode.
 *
 * Usage:
 *   bare-agent --jsonl --provider openai --model gpt-4o-mini
 *
 * Reads JSONL from stdin, writes JSONL events to stdout.
 * Cross-language bridge: any language spawns this process.
 *
 * ~40 lines target.
 */

// TODO: POC 5
console.error('bare-agent CLI not yet implemented — see POC 5');
process.exit(1);
