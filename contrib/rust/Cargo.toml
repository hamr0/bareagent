[package]
name = "bareagent"
version = "0.1.0"
edition = "2021"
description = "Thin subprocess wrapper for bare-agent"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
